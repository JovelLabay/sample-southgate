type Props = {
  name: string;
  statement: string;
  title: string;
  image: string;
  company: string;
};

export default function CarouselStatements({
  name,
  statement,
  title,
  image,
  company,
}: Props) {
  return <div>Statement</div>;
}
